1/ I restarted from scratch with Raquel's code: read_cta_4toy.c and C version of hessio: libhessio.so

1.1/ I transformed read_cta_4toy.c from executable to library with read_next_event function.

1.1.1/read_next_event function:
  Parse IO_BUFFER and return AllHessData (hess_all_data_struct)

hess_all_data_struct( from hessioxxx/include/io_hess.h):
   RunHeader run_header;
   MCRunHeader mc_run_header;
   CameraSettings camera_set[H_MAX_TEL];
   CameraOrganisation camera_org[H_MAX_TEL];
   PixelSetting pixel_set[H_MAX_TEL];
   PixelDisabled pixel_disabled[H_MAX_TEL];
   CameraSoftSet cam_soft_set[H_MAX_TEL];
   TrackingSetup tracking_set[H_MAX_TEL];
   PointingCorrection point_cor[H_MAX_TEL];
   FullEvent event;
   MCShower mc_shower;
   MCEvent mc_event;
   // MCpeSum mc_pesum;
   TelMoniData tel_moni[H_MAX_TEL];
   LasCalData tel_lascal[H_MAX_TEL];
   RunStat run_stat;
   MCRunStat mc_run_stat;

3/ What remains to be done:
- Still need to understand how event are construct from MC raw data, to return only full event
- pythonize the code to provide a generator

4/ Issue:
- swig do not generate a valid wrapped code (MCReader_wrap.c)
  hess_tel_event_adc_struct {
  ...
  uint16_t adc_sample[H_MAX_GAINS][H_MAX_PIX][H_MAX_SLICES]; ///< Pulses sampled.
  }
  _wrap_hess_tel_event_adc_struct_adc_sample_set 
   
  So after running swig, I need to modify by and the generated code MCReader_wrap.c and then compile and link.
